/* exported drop */
const drop = (array, count) => {
  const dropped = array.slice(count);
  return dropped;
};

// define a function with drop as the name and 2 params of array and count
// use the slice method on the array param with the argument of teh count param
// and assign it into a new variable dropped
// return dropped
