/* exported take */
const take = (array, count) => {
  const taken = array.slice(0, count);
  return taken;
};

// define a function take as name and 2 params of array and count
// use the slice method on the array with the arguments of 0 and count param
// and assign it into a new variable taken
// return taken
